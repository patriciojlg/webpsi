/**
 * loadColorPicker 
 * @since 1.2.2
 * @return void
 */
(function( $ ) {
 
    // Add Color Picker to all inputs that have 'color-field' class
    $(function() {
        $('.cn-at-color-iris').wpColorPicker();
    });
     
})( jQuery );